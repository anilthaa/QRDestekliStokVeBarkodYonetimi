@page "/"
@inject AuthStateService AuthState

<PageTitle>Ana Sayfa</PageTitle>

<h1>Hoş geldiniz@(AuthState.CurrentUser is null ? "" : ", " + AuthState.CurrentUser.Ad)</h1>

<p>QR Destekli Stok ve Barkod Yönetimi paneline başarıyla giriş yaptınız.</p>
